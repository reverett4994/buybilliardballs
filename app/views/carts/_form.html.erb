<%= form_for(@cart) do |f| %>
  <% if @cart.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@cart.errors.count, "error") %> prohibited this cart from being saved:</h2>

      <ul>
      <% @cart.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="hidden_field">
    <%= f.label :total %><br>
    <%= f.text_field :total %>
  </div>


  <%@ball=Ball.find(params[:ball])%>
  <h2>Are You Sure You Want To Add The <%=@ball.title%> to Your Cart?</h2>
  <div class="actions">
    <%= f.submit(balls: params[:ball] ) %>
  </div>
<% end %>
